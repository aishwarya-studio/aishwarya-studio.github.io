const rangoliImages = [];
const mehendiImages = [];
const drawingImages = [];
const decorationImages = [];

/* Rangoli Images */
for(let i=1; i<=35; i++){
    rangoliImages.push(`Media/Img/Rangoli/${i}.jpeg`);
}

/* Mehendi Images */
for(let i=1; i<=87; i++){
    mehendiImages.push(`Media/Img/Mehndi/${i}.jpeg`);
}

/* Drawing Images */
for(let i=1; i<=25; i++){
    drawingImages.push(`Media/Img/Drawing/${i}.jpeg`);
}

/* Decorations Images */
for(let i=1; i<=64; i++){
    decorationImages.push(`Media/Img/Decorations/${i}.jpeg`);
}